# Flutter Workspace

Este repositorio usa `C:\Users\nicol\Documents\Flutter` como carpeta raiz de trabajo.

## Estructura

- `wingets_app/`: proyecto Flutter principal (codigo fuente activo).

## Notas

- El proyecto ya no vive en la raiz del repo.
- CI ejecuta `analyze` y `test` dentro de `wingets_app/`.
